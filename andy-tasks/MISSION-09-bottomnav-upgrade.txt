Upgrade src/components/BottomNav.jsx — navigation bottom premium. Pill indicator animé : div absolue qui se déplace vers l'onglet actif (transition left/width 250ms ease, background --green-bg, border --border-hi, border-radius 999px). 5 icônes Lucide taille 22px : Home/TrendingUp/Globe/MoreHorizontal + icône centrale (Activity pour Sports). Labels 8px uppercase letterSpacing 0.08em. Safe-area-inset-bottom via padding. Badge rouge dynamique sur News si nouvelles non lues (count depuis context ou localStorage). Couleur inactive --t3, active --green. Hauteur 56px + safe area. Garde toute la logique de navigation existante.
