Améliore cli/andy-daemon.js : ajouter un système de priorité dynamique pour la queue. Au lieu de traiter les tâches par ordre alphabétique, implémenter une fonction priorityScore(filename) qui retourne un score numérique. Score basé sur : préfixe (NUIT-=100, v2-=80, manual-=60, auto-=40), présence de mots-clés dans le nom (redesign=+20, fix=+30, critical=+50, perf=+10). claimNextTask() trie par score décroissant avant de sélectionner. Logguer le score de chaque tâche claimée.
