Améliore la sécurité : 1) Dans vercel.json : ajoute headers de sécurité sur toutes les routes : X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy (camera=(), microphone=(), geolocation=()). 2) Dans api/_security.js (ou crée-le) : middleware qui vérifie que les clés API ne sont pas exposées, rate limiting simple par IP (max 100 req/min via Map en mémoire), sanitisation basique des inputs (strip tags HTML). 3) Vérifie que .env n'est pas dans le build Vite (vite.config.js envPrefix). 4) Dans src/utils/sanitize.ts : fonction sanitizeInput(str) qui échappe HTML entities.
