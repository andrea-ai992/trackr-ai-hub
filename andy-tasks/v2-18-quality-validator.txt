Améliore cli/andy-daemon.js fonction executeTask() : après la review Haiku (étape testing), ajouter une validation syntaxe basique en Node.js (pour les .jsx/.js : vérifier que les balises JSX sont fermées en comptant < vs >, vérifier pas de TODO/placeholder/lorem ipsum dans le code généré, vérifier que les imports référencés dans le code existent dans node_modules ou dans src/). Si validation échoue, logguer le problème précis et retry la génération avec un prompt plus strict incluant "ERREUR PRÉCÉDENTE: " + détail. Maximum 2 retries avant ERROR.
