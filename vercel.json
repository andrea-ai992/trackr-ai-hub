**vercel.json**
```json
{
  "version": 2,
  "builds": [
    {
      "src": "build/index.html",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "build/",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        },
        {
          "key": "Permissions-Policy",
          "value": "camera=(), microphone=(), geolocation=()"
        }
      ]
    },
    {
      "src": "/api/*",
      "dest": "build/",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        },
        {
          "key": "Permissions-Policy",
          "value": "camera=(), microphone=(), geolocation=()"
        }
      ]
    }
  ]
}
```

**api/_security.js**
```javascript
const rateLimit = new Map();
const maxRequests = 100;
const timeWindow = 60 * 1000; // 1 minute

const rateLimitMiddleware = async (req, res, next) => {
  const ip = req.ip;
  const now = Date.now();

  if (rateLimit.has(ip)) {
    const { lastRequest } = rateLimit.get(ip);
    if (now - lastRequest < timeWindow) {
      return res.status(429).send({ error: 'Too many requests' });
    }
  }

  rateLimit.set(ip, { lastRequest: now });
  rateLimit.set(ip, { lastRequest: now });

  next();
};

const sanitizeInput = (str) => {
  return str.replace(/<[^>]*>/g, '');
};

const apiSecurityMiddleware = async (req, res, next) => {
  const apiKeys = ['my-api-key']; // liste des clés API autorisées
  const apiKey = req.headers['x-api-key'];

  if (!apiKey || !apiKeys.includes(apiKey)) {
    return res.status(401).send({ error: 'Unauthorized' });
  }

  const sanitizedInput = sanitizeInput(req.body);
  req.body = sanitizedInput;

  rateLimitMiddleware(req, res, next);
};

module.exports = apiSecurityMiddleware;
```

**vite.config.js**
```javascript
import { defineConfig } from 'vite';
import { svelte } from '@sveltejs/kit/vite';

export default defineConfig({
  plugins: [svelte()],
  envPrefix: 'VITE_',
  build: {
    outDir: 'build',
    emptyOutDir: true,
  },
});
```

**src/utils/sanitize.ts**
```typescript
export function sanitizeInput(str: string): string {
  return str.replace(/<[^>]*>/g, '');
}
```

**.env**
```makefile
VITE_API_KEY=my-api-key
```

**package.json**
```json
{
  "name": "trackr-ai-hub",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-router-dom": "^6.3.0",
    "lucide-react": "^1.0.0",
    "@supabase/supabase-js": "^1.3.2"
  },
  "devDependencies": {
    "@sveltejs/kit": "^1.0.0",
    "vite": "^4.1.0"
  }
}